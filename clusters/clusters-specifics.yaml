apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cluster-specifics
  namespace: argocd
spec:
  ignoreApplicationDifferences:
    - jsonPointers:
        - /spec/syncPolicy
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - git:
      repoURL: https://github.com/jfr992/argocd-playground-migration.git
      revision: main
      directories:
      - path: clusters/*
  template:
    metadata:
      name: '{{.path.basename}}'
    spec:
      project: argocd
      source:
        repoURL: https://github.com/jfr992/argocd-playground-migration.git
        targetRevision: main
        path: '{{.path.path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: argocd
      syncPolicy:
        automated:
          prune: false
          selfHeal: true
